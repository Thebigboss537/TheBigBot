<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ganadores</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
            color: #333;
        }
        h1 {
            color: #2c3e50;
            text-align: center;
        }
        .winners-list {
            list-style-type: none;
            padding: 0;
            max-width: 600px;
            margin: 0 auto;
        }
        .winner-item {
            background-color: #fff;
            margin-bottom: 10px;
            padding: 15px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .consecutive {
            font-weight: bold;
            font-size: 1.2em;
            color: #3498db;
            margin-right: 10px;
            min-width: 30px;
            text-align: right;
        }
        .username {
            font-weight: bold;
            font-size: 1.2em;
            flex-grow: 1;
        }
        .score {
            background-color: #3498db;
            color: #fff;
            padding: 5px 10px;
            border-radius: 3px;
            font-weight: bold;
        }
        .no-winners {
            text-align: center;
            font-style: italic;
            color: #7f8c8d;
        }
    </style>
</head>
<body>
    <h1>Ganadores</h1>
    <% if (winners && winners.length > 0) { %>
        <ul class="winners-list">
        <% winners.forEach((winner, index) => { %>
            <li class="winner-item">
                <span class="consecutive"><%= index + 1 %>.</span>
                <span class="username"><%= winner.username %></span>
                <span class="score"><%= winner.homeScore %> - <%= winner.awayScore %></span>
            </li>
        <% }); %>
        </ul>
    <% } else { %>
        <p class="no-winners">No se encontraron ganadores.</p>
    <% } %>
</body>
</html>